{
  "name": "my-bass-tutor",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:smoke": "npm run build && node scripts/test-smoke-overlay.mjs",
    "check": "npm run test && npm run build"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.25.0",
    "jsdom": "^26.0.0",
    "playwright-core": "^1.58.2",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
